import express from "express";
import mongoose from "mongoose";
import jwt from "jsonwebtoken"
const app = express();


app.get("/api/v1/singup", (req, res) => {

});

app.post('/api/v1/singin', (req, res) => {

})